---
import BaseLayout from '@/layouts/BaseLayout.astro';
import { getCollection } from 'astro:content';
import type { GetStaticPaths } from 'astro';

export const getStaticPaths: GetStaticPaths = async () => {
  const posts = await getCollection('blog');
  const tags = new Set<string>();
  
  for (const post of posts) {
    for (const tag of post.data.tags || []) {
      tags.add(tag);
    }
  }

  return Array.from(tags).map(tag => ({
    params: { tag },
  }));
};

const { tag } = Astro.params;

const posts = (await getCollection('blog'))
  .filter(p => !p.data.draft && p.data.tags.includes(tag))
  .sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf());
---

<BaseLayout title={`分類：${tag}`} description={`瀏覽「${tag}」分類的所有文章`}>
  <header class="text-center space-y-4 mb-16">
    <div class="flex items-center justify-center gap-3">
      <span class="text-expo-muted text-body-md">分類</span>
      <h1 class="text-display-xl text-expo-ink inline">{tag}</h1>
    </div>
    <p class="text-body-md text-expo-muted">{posts.length} 篇文章</p>
  </header>

  <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 w-full max-w-5xl">
    {posts.map(post => (
      <article class="expo-card expo-card-hover flex flex-col h-full">
        <div class="flex-1">
          <time class="text-caption text-expo-muted">
            {post.data.date.toLocaleDateString('zh-TW', { year: 'numeric', month: 'long', day: 'numeric' })}
          </time>
          <h2 class="text-title-md text-expo-ink leading-tight mb-2 mt-2">
            <a href={`/blog/${post.slug}`} class="no-underline hover:text-expo-text-link">
              {post.data.title}
            </a>
          </h2>
          <p class="text-body-sm text-expo-body">{post.data.description}</p>
        </div>
        <div class="mt-6 pt-4 border-t border-expo-hairline">
          <a href={`/blog/${post.slug}`} class="text-expo-text-link no-underline text-button font-medium hover:underline">
            閱讀更多 &rarr;
          </a>
        </div>
      </article>
    ))}
  </div>

  {posts.length === 0 && (
    <div class="text-center py-16 text-expo-muted text-body-md">
      此分類尚無文章
    </div>
  )}

  <div class="mt-16 text-center">
    <a href="/tags" class="nav-link no-underline">
      &larr; 回到分類列表
    </a>
  </div>
</BaseLayout>
